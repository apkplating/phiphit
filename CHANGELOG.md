# CHANGELOG — APK Plating Web Suite

> **วิธีใช้ไฟล์นี้**
> - แนบไฟล์นี้มาด้วยทุกครั้งที่เปิดแชทใหม่กับ Claude เพื่อขอแก้โค้ด (พร้อมกับไฟล์ .html ที่จะแก้)
> - ทุกอย่างที่ทำเครื่องหมาย **✅ เสร็จสมบูรณ์** คือจุดที่ตรวจสอบแล้วว่าใช้งานได้ตามต้องการ — ห้ามแก้/ลบ/เปลี่ยน behavior โดยไม่ได้รับอนุญาตชัดเจน
> - เมื่อ Claude แก้ฟีเจอร์ใหม่เสร็จและทดสอบแล้วว่าใช้ได้ ให้เพิ่มบรรทัดใหม่ต่อท้ายไฟล์นี้ (อย่าลบของเก่า) พร้อมวันที่และไฟล์ที่แก้
> - ถ้าเจอว่าโค้ดจริงกับ CHANGELOG ไม่ตรงกัน (ฟีเจอร์ที่บอกว่าเสร็จแล้วแต่หายไปจากโค้ด) ให้หยุดถามผู้ใช้ก่อน อย่าเดาเอง

---

## รูปแบบบันทึกแต่ละรายการ
```
### [ไฟล์] ชื่อฟีเจอร์/สิ่งที่แก้ — วันที่ (สถานะ)
รายละเอียดสั้นๆ ว่าทำอะไร ตรรกะสำคัญคืออะไร จุดที่ต้องระวังถ้าจะแก้ต่อ
```

---

## Core Architecture (คงที่มาตั้งแต่เริ่มต้น)
- Static HTML + vanilla JS โฮสต์บน GitHub Pages — repo: `apkplating/phiphit`
- Firebase Realtime Database แยกตามสาย: primary `platingapp-92e21-c1346` (ใช้ร่วมกับ B1), `platingapp-b2` (B2), `platingapp-r1` (R1)
- ระบบสิทธิ์กลาง: `auth-central.js` (CentralAuth module), เก็บ role matrix ที่ `auth_admin.html`, Firebase path `/auth-central.json`
- ฟอนต์ IBM Plex Sans Thai, ธีมสี navy/steel
- แนวทางแก้โค้ด: **surgical minimal diff** ไม่ rewrite ทั้งไฟล์, เช็ค syntax ด้วย Node.js ก่อนส่งมอบเสมอ, เช็ค div/table tag balance ด้วย Python, ไฟล์ส่งมอบไปที่ `/mnt/user-data/outputs/`

---

## [wip-calc.js] Shared WIP calculation module — ✅ เสร็จสมบูรณ์
โมดูลกลางอ่านค่าจาก `dayRecords` ตรงๆ ในทุกโปรเจกต์ Firebase (B1/B2/R1) เพื่อให้ยอดสต็อกคงเหลือตรงกันทุกที่ที่เรียกใช้ (bl_stock.html, production_order.html, plating_v4/5/6)

## [plating_v4/v5/v6, bl_stock.html] Bill overage & NOBILL — ✅ เสร็จสมบูรณ์
- **Hard-block** เมื่อกรอกจำนวนเกินยอดบิล (ป้องกันตัดสต็อกเกิน)
- **NOBILL ต้องขออนุมัติ** ก่อนตัดสต็อก ไม่ silent-deduct แบบเดิม — มีระบบสิทธิ์อนุมัติละเอียด (`auth-central.js` / `auth_admin.html`)
- หลักการ: overage = บล็อกทันที, NOBILL/ไม่มีสต็อก = ต้องอนุมัติก่อน (ไม่ใช่บล็อกเฉยๆ)

## [job_master.html, bl_stock.html] Firebase PUT vs PATCH bug — ✅ เสร็จสมบูรณ์ (แก้ถาวรแล้ว)
บั๊กร้ายแรง: ใช้ PUT แทน PATCH ทำให้ข้อมูล invoice/customer/priceList ถูกเขียนทับหายไปทั้ง field เพราะ race condition — แก้ด้วย flag `_hasSuccessfulCloudSync` และ localStorage backup guard ใน job_master.html **ห้ามเปลี่ยนกลับไปใช้ PUT ตรงจุดนี้เด็ดขาด**

## [bl_stock.html] STOCK1/STOCK2/STOCK3 traceability — ✅ เสร็จสมบูรณ์
แก้บั๊กหลายชั้น: นับ delivery ซ้ำสอง, wipMap ตัดข้อมูล 30 วันทำให้ STOCK2 เป็นศูนย์ผิด, partName key ไม่ตรงกัน (bare vs concatenated), NOBILL โดนรวมเข้าไปในยอด plated ผิด — เพิ่ม live WIP ผ่าน live.json และ verify bar

## [job_master.html] ใบส่งคืนงานเคลม (Claim Return Document) — ✅ เสร็จสมบูรณ์
- ระบบเลขที่เอกสาร prefix `CL`
- เลือกลูกค้าก่อนในโหมดเคลม, template พิมพ์แยกเฉพาะ
- **23 ก.ค. 2569**: ปรับขนาดกระดาษพิมพ์เป็นกระดาษต่อเนื่อง 9x5.5 นิ้ว (`@page{size:9in 5.5in}`), ลด blank rows จาก 6 เหลือ 3 แถวให้พอดีหน้า
- **23 ก.ค. 2569**: คอลัมน์ "จำนวน" แสดง `<จำนวนชิ้น> ชิ้น (<น้ำหนัก กก.>)` โดยคำนวณน้ำหนักจาก Part Master (`pmPart.weightPerPc` กรัม/ชิ้น × จำนวนชิ้น ÷ 1000) ไม่ใช้ `l.qty` ตรงๆ (เพราะอาจไม่ตรงกับน้ำหนัก/ชิ้นจริงปัจจุบันใน PM) — fallback ไป `l.qty` ถ้า PM ไม่มีข้อมูลน้ำหนัก — ใช้ฟังก์ชัน `invLineQtyDisplay()` ร่วมกับ `printInvoice()`/`printInvoiceOnForm()` ด้วย

## [production_order.html] BL Stock → เข้าคิว → ใบสั่งผลิต — ✅ เสร็จสมบูรณ์ (23 ก.ค. 2569)
- เปลี่ยน behavior ปุ่ม "➕ เข้าคิว" ในตาราง "รายการรอชุบจาก BL Stock": **เดิม** ดันเข้า Backlog แล้วรอ Auto Scheduler (05:00 หรือกดรันเอง) ตัดกำลังการผลิตก่อนออก PO → **ใหม่** สร้างใบสั่งผลิต (PO) ให้ทันทีที่กด ไม่เช็คกำลังการผลิตสูงสุด (เพราะผู้ใช้กดยืนยันเองทีละแถว/บิลอยู่แล้ว) โผล่ที่ตาราง "📜 ใบสั่งผลิตทั้งหมด" ทันที
- รายการที่ยังชุบไม่ครบยอดจะกลับมาโชว์ในตาราง BL Pending เองอัตโนมัติ (คำนวณสดจากข้อมูลจริงทุกครั้งที่กด "ดึงข้อมูลจาก BL Stock" — ไม่ต้องเขียนโค้ดเพิ่ม เพราะระบบ compute ใหม่จากล็อต vs WIP จริงเสมอ)
- **หมายเหตุค้างอยู่**: ตาราง "🗂️ คิวรอผลิต (Backlog)" กับปุ่ม "▶ รันจัดคิวเดี๋ยวนี้" ยังไม่ได้ลบออกจากหน้า เพราะไม่แน่ใจว่า GitHub Actions script (รันทุกวัน 05:00) ยังพึ่งพา `production-backlog.json` อยู่หรือไม่ — ตอนนี้ตารางนี้จะว่างเปล่าเสมอเพราะไม่มีอะไรป้อนเข้าไปแล้ว ยังไม่ตัดสินใจว่าจะลบทิ้งหรือไม่

## [plating_v4.html] Chemical Movement Table (Auto/Manual/AH/Month filter) — ✅ เสร็จสมบูรณ์ (23 ก.ค. 2569)
ตาราง "📋 รายการเคลื่อนไหว" ในแท็บเคมี:
- เดิมโชว์แค่ยอดเบิกแบบ Auto (คำนวณจาก AH) เท่านั้น — รายการเบิก Manual (เติมเคมีพิเศษ, เก็บใน `chemRecords` type `out`) ไม่เคยโผล่ในตารางนี้เลย (มีแค่รวมยอดในสรุปด้านบน)
- **แก้แล้ว**: ดึง Auto (จาก `dayRecords`) + Manual (จาก `chemRecords`) มารวมกัน เรียงตามวันที่จริง แยกคอลัมน์ "เบิก Auto (ลิตร)" กับ "เบิก Manual (ลิตร)" ชัดเจน ไม่ปนกัน
- เพิ่มคอลัมน์ **AH** (โชว์ค่ากระแสไฟที่ใช้คำนวณยอด Auto วันนั้น)
- เพิ่ม **ตัวกรองเดือน** (dropdown สร้าง option อัตโนมัติจากเดือนที่มีข้อมูลจริง) — running balance ยังคำนวณจากประวัติทั้งหมดเสมอ ไม่ได้ reset ตามเดือนที่กรอง
- เพิ่ม **แถวรวมท้ายตาราง** สรุปยอดรับเข้า/AH รวม/เบิก Auto รวม/เบิก Manual รวม/คงเหลือล่าสุด ของช่วงที่กรองอยู่
- **23 ก.ค. 2569 (เพิ่มเติม)**: แก้บั๊กตัวกรองเดือน — popup ของ `<select id="chemMonthFilter">` ตอนกดเปิดดูตัวเลือก ตัวหนังสือกลายเป็นสีขาวบนพื้นขาว (มองไม่เห็น) เพราะ `<option>` inherit สี `color:#fff` จาก select ที่ตั้งใจไว้สำหรับตอนปิด (แสดงบนแถบ navy) — แก้ด้วย CSS rule `#chemMonthFilter option{color:#0f172a;background:#fff;}` แยกสีของ popup ออกจากกล่องตอนปิดอย่างชัดเจน **สำคัญ:** rule นี้ผูกกับ `id="chemMonthFilter"` — ถ้าจะเพิ่ม dropdown อื่นที่ใช้ธีมเดียวกัน (พื้นเข้ม/ตัวหนังสือขาวตอนปิด) ต้องเพิ่ม CSS rule แยกสำหรับ id นั้นด้วย ไม่ได้ apply อัตโนมัติกับ select ทุกตัว

## [plating_v5_B2.html, plating_v6_R1.html] พอร์ต Chemical Movement Table จาก v4 — ✅ เสร็จสมบูรณ์ (23 ก.ค. 2569)
พอร์ตฟีเจอร์เดียวกับ v4 (Auto/Manual แยกคอลัมน์, AH, ตัวกรองเดือน, แถวรวมท้ายตาราง, แก้สี dropdown popup) มาให้ v5 (B2) และ v6 (R1) ครบทั้งคู่ — ปรับให้เข้ากับโครงสร้างเดิมของแต่ละไฟล์ที่ต่างจาก v4 เล็กน้อย:
- v5/v6 ฝั่ง "รับเข้า" คือ **จัดซื้อโดยตรง** (`chemRecords` type `in`) ไม่ใช่เบิกจาก Master Stock เหมือน v4 — โครงสร้างคอลัมน์ฝั่งนี้ (เลขที่ใบรับงาน, รับเข้า(ถัง), ผู้บันทึก) คงไว้ตามเดิม ไม่ได้แตะ
- `calcDayChemUsedLiter()` ของ v5/v6 มี logic กรองตาม **บ่อ (batch)** ที่ v4 ไม่มี — คงไว้ตามเดิม ไม่ได้เปลี่ยน
- โครงสร้างข้อมูล manual dosing (`chemRecords` type `out`, `isManual:true`) เหมือนกันทั้ง 3 ไฟล์ ใช้ logic รวม Auto+Manual แบบเดียวกันได้เลย

## [plating_v4.html, plating_v5_B2.html, plating_v6_R1.html] แก้คอลัมน์ "AH" ให้ถูกต้องตามประเภทการคำนวณของแต่ละเคมี — ✅ เสร็จสมบูรณ์ (23 ก.ค. 2569)
บั๊ก: คอลัมน์ "AH" ในตาราง "รายการเคลื่อนไหว" เดิมโชว์ค่า AH ของวันนั้นให้ **ทุกแถว Auto เหมือนกันหมด** ทั้งที่เคมีบางตัว (เช่น ALX-441A ในกลุ่ม Black Chromate) จริงๆ คำนวณยอดใช้จาก **จำนวนโม่ต่อสี** (K/B/C) ไม่ใช่ AH — ทำให้ตัวเลขที่โชว์ไม่ตรงกับที่ใช้คำนวณจริง สร้างความสับสน
- **แก้แล้ว**: เพิ่มฟังก์ชัน `calcDayChemRef(day)` คู่กับ `calcDayChemUsedLiter(day)` เดิม — เช็คจาก `chemSettings` ว่าเคมีตัวนี้ผูกกับ group/sub-chemical แบบ `type:'AH'` หรือ `type:'COLOR'` แล้วคืนค่าอ้างอิงที่ตรงกับสูตรคำนวณจริงของเคมีนั้น (AH หรือจำนวนโม่ของสีที่ตรงกัน)
- คอลัมน์เปลี่ยนชื่อเป็น **"AH/โม่"** และแสดงผลต่างกันตามประเภท: เคมีที่คิดจาก AH → โชว์ "<เลข> AH", เคมีที่คิดจากโม่ → โชว์ "<เลข> โม่ (<สี>)" เช่น "5 โม่ (K ดำ)"
- แถวรวมท้ายตาราง ("AH รวม") **สรุปเฉพาะแถวที่เป็น AH-type จริงเท่านั้น** ไม่รวมยอดโม่เข้าไปด้วย (หน่วยต่างกัน รวมกันแล้วความหมายผิด)
- **23 ก.ค. 2569 (เพิ่มเติม)**: ตอนเพิ่มแถวรวมครั้งแรก ลืมรวมยอดสำหรับเคมีกลุ่ม COLOR-type (ที่คิดจากจำนวนโม่) ทำให้ช่องนี้ว่างเปล่าเมื่อรายการทั้งหมดในเดือนนั้นเป็นแบบโม่ (ไม่มี AH เลย) — แก้เพิ่ม `sumMolds`/`sumMoldsColor` แยกจาก `sumAH` เพื่อสรุปยอดโม่รวมของสีนั้นด้วย (เคมีตัวหนึ่งมีสีเดียวคงที่ทั้งตาราง จึงรวมกันได้ตรงความหมาย ไม่ปนข้ามสี)
- v5/v6 (มี concept "บ่อ/batch") ใช้ logic เดียวกับ `calcDayChemUsedLiter` เดิมที่กรองตาม `item.batches` ก่อนนับจำนวนโม่ ให้ตรงกับสูตรคำนวณจริงของแต่ละไฟล์

## [plating_v4.html, plating_v5_B2.html, plating_v6_R1.html] 🔴 แก้บั๊กใหญ่: การจับคู่ "สี" ของเคมีกลุ่ม COLOR ผิดมาตั้งแต่ต้น — ✅ เสร็จสมบูรณ์ (23 ก.ค. 2569)
**นี่คือบั๊กที่มีมาก่อนหน้าเซสชันนี้แล้ว ไม่ใช่บั๊กที่เพิ่งเกิดจากการแก้ล่าสุด** — พบระหว่างตรวจสอบตามที่ผู้ใช้ทักท้วงว่าคอลัมน์ "AH/โม่" ของ ALX-441A ควรโชว์ "(K)" ไม่ใช่ "(ทุกสี)"

**สาเหตุที่แท้จริง**: ตามโครงสร้างข้อมูลจริงของ `chemSettings` (ดู `saveChemSettings()`) — `{ name, type:'AH'|'COLOR', color:'B'|'C'|'K', chemicals:[{name, rate}] }` — **สีถูกเก็บไว้ที่ระดับกลุ่ม (`c.color`) ค่าเดียวใช้ร่วมกันทั้งกลุ่ม** (เลือกผ่าน radio "ใช้กับสี" ตอนตั้งค่า) ไม่ได้เก็บแยกรายสารย่อย (`ch.color` **ไม่มีอยู่จริง** ในโครงสร้างข้อมูล เป็น `undefined` เสมอ)

แต่ทุกฟังก์ชันที่คำนวณยอดใช้เคมีของกลุ่ม COLOR กลับไปอ่าน `ch.color` (สารย่อย) แทนที่จะเป็น `c.color` (กลุ่ม) — ทำให้ `color` เป็นค่าว่างเสมอ แล้วตกไปที่ branch `else` ซึ่ง **รวมจำนวนโม่ทุกสี (B+C+K) เข้าด้วยกัน** ทั้งที่ควรใช้แค่จำนวนโม่ของสีที่กลุ่มนั้นตั้งไว้เท่านั้น ผลคือ **ยอดใช้เคมีของทุกกลุ่ม COLOR-type ที่มีสารย่อยถูกคำนวณสูงเกินจริง** (สูงกว่าที่ควรเป็น ~2-3 เท่า ถ้าโม่กระจายทั้ง 3 สี) ตลอดมา

**จุดที่แก้ (5 ฟังก์ชัน × 3 ไฟล์ = 15 จุด)**:
1. `calcAutoChemUsed()` — ยอด autoOut ที่ใช้แสดงในสรุปด้านบนการ์ด (คงเหลือ/เบิกจากระบบ) → เปลี่ยนเป็น `matchedGroup.color`
2. `calcDayChemUsedLiter()` — ยอดเบิกรายวันในตาราง "รายการเคลื่อนไหว" (ที่ใช้คำนวณ running balance ด้วย) → เปลี่ยนเป็น `c.color`
3. `calcDayChemRef()` — ฟังก์ชันใหม่ที่เพิ่งเพิ่มไปเมื่อกี้ (คอลัมน์ AH/โม่) → เปลี่ยนเป็น `c.color`
4. `calcChemMonthTotal()` — ยอดรวมเคมีสะสมรายเดือน (การ์ด "สรุปเคมีสะสมทั้งหมด") → เปลี่ยนเป็น `c.color`
5. `calcAutoChemUsed_forDay()` — ยอดใช้ต่อ item ต่อเดือน (ตาราง breakdown รายเดือน) → เปลี่ยนเป็น `c.color`

**ยืนยันวิธีที่ถูกต้องจาก**: โค้ดส่งรายงาน Discord (บรรทัด ~2117) ที่อ่าน `barrelMap[c.color]` ถูกต้องอยู่แล้ว — ใช้เป็นต้นแบบยืนยันว่าโครงสร้างจริงคือ group-level color

**ผลกระทบหลังแก้**: เพราะเป็นการคำนวณสดจาก `dayRecords` ทุกครั้ง (ไม่มีข้อมูลถูกเขียนทับเสียหาย) แค่แก้โค้ดตัวเลขก็ถูกต้องทันทีโดยไม่ต้อง migrate ข้อมูลเก่า — แต่ **ตัวเลข "คงเหลือ" และยอดใช้เคมีของกลุ่ม COLOR-type ทุกตัวจะเปลี่ยนไป (ลดลง) ทันทีที่เปิดหน้าเว็บเวอร์ชันใหม่** ผู้ใช้ควรตรวจสอบยอดคงเหลือเคมีอีกครั้งหลังอัปเดต

## [plating_v4.html, plating_v5_B2.html, plating_v6_R1.html] 🔴 แก้บั๊กใหญ่ #2: จับคู่ชื่อเคมีไม่เจอเพราะช่องว่างไม่ตรงกัน — ✅ เสร็จสมบูรณ์ (23 ก.ค. 2569)
พบระหว่างตรวจ: เคมี **HYPRO200** (ชื่อใน Chem Master, code C-005) ไม่มีข้อมูล "ผลิต (เบิกใช้)" ในตารางเคลื่อนไหวเลย ทั้งที่อยู่กลุ่ม Black Chromate (K ดำ) เดียวกับ ALX-441A/ALX-441B ที่มีข้อมูลปกติ — เช็คแล้วพบว่าชื่อที่ตั้งไว้ใน `chemSettings` (หน้าตั้งค่าเคมี) สะกดว่า **"HYPRO 200"** (มีช่องว่าง) แต่ชื่อจริงใน Chem Master คือ **"HYPRO200"** (ไม่มีช่องว่าง) — ทุกฟังก์ชันเทียบชื่อด้วย `.trim().toLowerCase()` เฉยๆ (ตัดแค่ช่องว่างหัวท้าย ไม่ตัดช่องว่างกลางคำ) จึงมองว่าเป็นคนละชื่อกัน จับคู่ไม่เจอ คำนวณยอดใช้ออกมาเป็น 0 เสมอ

**แก้แล้ว**: เพิ่มฟังก์ชัน `chemNameKey(s)` — normalize ชื่อโดยตัดช่องว่างทุกจุดออก (ไม่ใช่แค่หัวท้าย) ก่อนเทียบ แทนที่ `.trim().toLowerCase()` แบบเดิมในทุกจุดที่ใช้เทียบชื่อเคมีสำหรับคำนวณยอดใช้ (ไม่แตะจุดที่ใช้ track การเปลี่ยนชื่อ/rename ซึ่งควร match แบบเป๊ะอยู่แล้ว) ครอบคลุม `calcAutoChemUsed`, `calcDayChemUsedLiter`, `calcDayChemRef`, `calcAutoChemUsed_forDay`, และ `exportChemCSV`
**พบเพิ่มระหว่างแก้**: `exportChemCSV` เดิมยิ่งแย่กว่านั้น — เทียบแค่ชื่อกลุ่ม (`c.name`) ไม่เคยเช็คชื่อสารย่อย (`chemicals[].name`) เลย ทำให้ CSV export ของเคมีที่เป็นสารย่อย (เช่น ALX-441A/B, HYPRO200) ออกมาเป็น 0 เสมอไม่ว่าชื่อจะตรงกันหรือไม่ — แก้ให้ match สารย่อยได้เหมือนฟังก์ชันอื่นแล้ว
**ข้อควรระวังสำหรับผู้ใช้**: บั๊กนี้แก้ได้แค่ "การจับคู่" — ถ้าใน Chem Master กับหน้าตั้งค่าเคมียังมีชื่อสะกดต่างกันแบบอื่นที่ไม่ใช่แค่ช่องว่าง (เช่น ตัวสะกดผิด, ใช้ตัวย่อคนละแบบ) จะยังจับคู่ไม่เจอเหมือนเดิม — แนะนำเช็คว่าชื่อเคมีทุกตัวใน Chem Master ตรงกับชื่อที่ตั้งไว้ในหน้าตั้งค่าเคมี (chem settings) แบบตัวต่อตัว

## [job_master.html] เพิ่มฟีเจอร์ "🧹 ปิดบิลเก่าเป็นชุด" (Bulk Close) — ✅ เสร็จสมบูรณ์ (23 ก.ค. 2569)
ปัญหาที่พบ: มีบิลรับเข้าเก่าจำนวนมาก (ก่อนมีระบบตัดสต็อก) ที่ "จ่ายออก=0" ตลอดแม้เกินกำหนดมาหลายสิบวัน เพราะของถูกใช้ไปจริงแล้วแต่ไม่เคยมี INV ผูกไว้ให้ตัดยอด ทำให้ "คงเหลือ" ในหน้า job_master.html โชว์เต็มจำนวนผิดๆ (พบเคสจริง P/No เดียวมีบิลค้างแบบนี้เป็นสิบใบ อายุ 1–33+ วัน)
**แก้แล้ว**: เพิ่มปุ่ม "🧹 ปิดบิลเก่าเป็นชุด" ที่แท็บภาพรวม (ข้าง CSV) เปิด modal ให้:
1. กรอกจำนวนวัน (default 30) — ค้นหาบิลที่รับเข้ามาแล้วเกิน N วัน และยังมีคงเหลือ > 0
2. โชว์ preview ตาราง (เลขบิล/ชิ้นงาน/ลูกค้า/อายุ/คงเหลือ) พร้อม checkbox เลือกได้เป็นรายบิล (default เลือกทั้งหมด) ก่อนยืนยัน — ไม่ใช่กดแล้วตัดทันทีแบบไม่มีจุดตรวจสอบ
3. ยืนยันแล้วมี native `confirm()` popup ซ้ำอีกชั้นกันกดพลาด
**ตรรกะการปิดบิล** (`bulkCloseBill()`): ตั้ง `job.qty`/`job.qtyKg` ให้เท่ากับ `totalOut` ที่คำนวณจาก `calcJob()` (ยอดที่เคยออก INV จริงแล้วเท่านั้น) — **ไม่ใช่ตั้งเป็น 0 เสมอไป** เผื่อบิลนั้นมีการออก INV จริงบางส่วนไปแล้ว จะปิดแค่ส่วนคงเหลือ ไม่ไปลบยอดที่ขายจริงออก ผลคือ `remain = totalIn - totalOut = 0` พอดี — อัปเดต `jobRecords` type `in` ตัวแรกให้ตรงกันด้วย พร้อม tag หมายเหตุ `[ปิดบิลอัตโนมัติ <วันที่> — ข้อมูลเก่าก่อนมีระบบตัดสต็อก]` ต่อท้ายทุกบิลที่ปิด เพื่อสอบกลับได้ภายหลัง
**ไม่กระทบ**: ไม่แตะ `blInvoices`/INV ที่ออกไปแล้วเลย เป็นการแก้แค่ "จำนวนรับเข้าที่บันทึกไว้" ให้ตรงกับความจริงเท่านั้น — แก้ไขย้อนกลับได้ทีหลังผ่าน "✏️ แก้ไขข้อมูลบิล" ถ้าปิดผิดบิล
**23 ก.ค. 2569 (แก้บั๊กต่อเนื่อง)**: หลังทดสอบจริง (ปิดไป 629 บิล) พบว่าบิลที่ปิดแล้ว (ตัด qty เหลือ 0) ยัง**ค้างสถานะ "เกินกำหนด" (สีแดง) ตลอดไป ไม่เปลี่ยนเป็น "จ่ายครบ"** — สาเหตุ: `getStatus()` เดิมเช็ค `isDone` จากเงื่อนไข `remain<=0 && totalIn>0` แต่บิลที่ปิดแล้วมี `totalIn=0` ด้วย (เพราะ qty เดิมก็คือ totalIn) เงื่อนไขนี้จึงไม่มีวันเป็นจริง ตกไปเช็คจำนวนวันแทน ได้ "เกินกำหนด" ทุกครั้ง
**23 ก.ค. 2569 (แก้เพิ่มอีกรอบ — root cause จริง)**: แก้แบบ flag `closed`+เช็ค note ไปก่อนหน้านี้ยังไม่ครอบคลุมทุกเคส — เจอบิลอื่นที่ `totalIn=0` มาแต่ต้น (ไม่ได้ผ่านการปิดบิลเลย เช่นบิล `0000024112`) ก็ค้าง "เกินกำหนด" เหมือนกัน เพราะสาเหตุคือเงื่อนไข `totalIn>0` เอง ไม่ใช่แค่กรณีปิดบิล — **แก้ที่ต้นตอจริง**: ตัดเงื่อนไข `totalIn>0` ออกจาก `isDone` ทั้งหมด เหลือแค่ `remain<=0` → บิลไหนคงเหลือ ≤ 0 ไม่ว่าจะมาจากสาเหตุอะไร (ปิดบิลอัตโนมัติ/แก้ไขเอง/กรอกไว้เป็น 0 แต่แรก) ถือว่า "จ่ายครบ" เสมอ ไม่มีทางค้าง "เกินกำหนด" อีก (แก้ครั้งก่อนหน้าที่เช็ค `job.closed`/note ยังคงไว้เผื่อกรณีอื่น แต่ไม่ใช่ตัวตัดสินหลักอีกต่อไป)
ผลคือ summary chip "จ่ายครบ"/"เกินกำหนด" ที่ด้านบนก็ถูกต้องตามไปด้วย (คำนวณจาก `getStatus()` เดียวกัน)

## [job_master.html] รวมคอลัมน์ "คงเหลือ" + สีจำนวนชิ้น/กก. ในตารางภาพรวม — ✅ เสร็จสมบูรณ์ (23 ก.ค. 2569)
เดิมตารางบอร์ดมีคอลัมน์ "คงเหลือ" แยกเป็น 2 คอลัมน์ (ชิ้น) กับ (กก.) ต่างหาก ไม่เหมือนคอลัมน์ "รับเข้า"/"จ่ายออก" ที่โชว์หน่วยหลัก+หน่วยรองในวงเล็บรวมอยู่คอลัมน์เดียว — แก้ให้เป็นรูปแบบเดียวกัน: รวมเหลือคอลัมน์เดียว โชว์หน่วยหลักตามบิล (ชิ้น หรือ กก.) ตัวใหญ่ ตามด้วยหน่วยรอง (แปลงผ่านน้ำหนัก/ชิ้นจาก Part Master) ในวงเล็บตัวเล็ก ปรับ `<th>` และ logic การ build cell ใน `renderBoard()` — CSV export (`exportBoardCSV()`) ไม่แตะ ยังคงแยกคอลัมน์ชิ้น/กก. เหมือนเดิมเพื่อประโยชน์ตอนเอาไปวิเคราะห์ต่อ

**23 ก.ค. 2569 (เพิ่มสี)**: ขยายให้ทั้ง 3 คอลัมน์ **รับเข้า / จ่ายออก / คงเหลือ** ใช้ตัวสร้าง cell ตัวเดียวกัน (`qtyCell()`) หน้าตาสอดคล้องกันหมด และกำหนดสีตายตัวตามชนิดหน่วยเสมอ ไม่ว่าจะเป็นหน่วยหลักหรือหน่วยรอง:
- **ชิ้น = สีน้ำเงิน** (`#1d4ed8`)
- **กก. = สีแดง** (`#dc2626`)

ทุกแถวพยายามแปลงให้เห็นครบทั้ง 2 หน่วยเสมอ (ผ่าน `weightPerPc` จาก Part Master) **ข้อจำกัด**: ถ้า Part Master ของชิ้นงานนั้นไม่มีข้อมูล `weightPerPc` จะแปลงหน่วยรองไม่ได้ (โชว์แค่หน่วยหลักหน่วยเดียว) เพราะไม่มีอัตราแปลงให้คำนวณ

**แก้เพิ่มเติมระหว่างทำ**: พบว่า `isDone` (ตัวกำหนดว่าคอลัมน์ "วันค้าง" ควรโชว์ "—" หรือไม่) ในฟังก์ชัน `renderBoard()` คำนวณแยกจาก `getStatus()` และยังมีเงื่อนไข `totalIn>0` แบบเดียวกับบั๊กที่เพิ่งแก้ไปก่อนหน้า — เปลี่ยนให้ใช้ผลจาก `getStatus(job)==='จ่ายครบ'` แทนการคำนวณซ้ำ ป้องกันบั๊กแบบเดียวกันเกิดซ้ำที่จุดอื่นในอนาคต

## [job_master.html] 🔴 แก้บั๊กใหญ่ #3: `pullFromLines()` ตัดสต็อกซ้ำหลายบิล + ไม่เรียง FIFO — ✅ เสร็จสมบูรณ์ (23 ก.ค. 2569)
พบระหว่างไล่หาสาเหตุที่บิลเก่าหลายใบของ P/No เดียวกัน (เช่น 7G-3101-12) มีคงเหลือเป็นเศษกระจัดกระจายแบบสุ่ม (154, 400, 3250, 831, 1900...) ทั้งที่ไม่เคยผ่านการปิดบิลเลย

**สาเหตุที่แท้จริง**: `pullFromLines()` (ทำงานอัตโนมัติทุก ~60 วินาทีผ่าน `startPoll()`) ดึงรอบชุบจริงจาก B1/B2/R1 มาสร้าง jobRecords type `out` ("Auto จากรอบชุบ") ให้อัตโนมัติ — เดิมโค้ดวน `jobItems.forEach` แล้ว**สร้างรายการตัดสต็อกแยกให้ทุกบิลที่ P/No ตรงกันและยังมีคงเหลือ โดยตัดเต็มจำนวนของรอบชุบนั้นซ้ำให้แต่ละบิล** — ถ้า P/No หนึ่งมีบิลค้าง 5 ใบพร้อมกัน (พบบ่อยมากในระบบนี้) รอบชุบ 1 รอบจะกลายเป็นถูกตัดออกจาก Job Master **5 เท่า** ของที่ผลิตจริง (นับซ้ำ) แถมไม่มีลำดับแน่นอนว่าตัดบิลไหนก่อน (เรียงตาม array order ของ `jobItems` ซึ่งไม่ใช่ลำดับวันที่)

**แก้แล้ว**: เปลี่ยนเป็น**ตัดแบบ FIFO** — เรียงบิลที่ P/No ตรงกันตามวันที่รับเข้า **เก่าสุดก่อนเสมอ** แล้ว "แบ่ง" จำนวนของรอบชุบนั้นไล่ตัดทีละบิลจนกว่าจะครบยอด (ถ้ารอบนั้นมากกว่าคงเหลือของบิลแรก จะตัดบิลแรกจนหมดพอดีแล้วไล่ตัดบิลถัดไปต่อ) ยอดตัดรวมทั้งหมดจะเท่ากับจำนวนที่ผลิตจริงในรอบนั้นเป๊ะ ไม่มีทางเกิน — เพิ่ม tag `(FIFO)` ต่อท้ายหมายเหตุของ record ที่สร้างจากตรรกะใหม่นี้ ให้แยกจาก record เก่าที่สร้างก่อนแก้ได้
**ผลกระทบ**: หลังแก้ ยอด "จ่ายออก"/"คงเหลือ" ของบิลเก่าที่มี P/No ซ้ำกันหลายใบจะเปลี่ยนไป (ลดลง เพราะไม่นับซ้ำแล้ว) ทันทีที่ `pullFromLines()` รันรอบถัดไป (~60 วินาที) — **jobRecords เก่าที่สร้างไปแล้วก่อนแก้ (นับซ้ำอยู่) ยังคงอยู่ในระบบ ไม่ได้ลบออกให้อัตโนมัติ** เพราะการลบ record ย้อนหลังมีความเสี่ยงเกินไปที่จะทำแบบอัตโนมัติ ต้องพิจารณาเป็นกรณีไปว่าจะเคลียร์ยังไง (อาจใช้ "🧹 ปิดบิลเก่าเป็นชุด" ไล่เคลียร์บิลที่ได้รับผลกระทบต่อ)

## [job_master.html] ล็อกคงเหลือ=0 ถาวรสำหรับบิลที่ปิดแล้ว + เพิ่มปุ่มปลดล็อก — ✅ เสร็จสมบูรณ์ (23 ก.ค. 2569)
ผู้ใช้ถามว่าบิลที่ปิดไปแล้วมีทางกลับมาโชว์คงเหลือได้อีกไหม — พบว่ามีช่องโหว่ทางทฤษฎี: `calcJob()` คำนวณ `remain = totalIn - totalOut` สดทุกครั้ง ถ้ามีใครไปแก้ไข/ลบ INV ที่เคยผูกกับบิลที่ปิดไปแล้วทีหลัง (ทำให้ `totalOut` ที่เคยตั้งไว้ตอนปิดบิลลดลง) คงเหลือจะคำนวณกลับมาเป็นบวกได้อีกโดยไม่ตั้งใจ
**แก้แล้ว**: เพิ่มเงื่อนไขใน `calcJob()` — ถ้า `job.closed === true` ให้ล็อก `remain`/`remainKg` เป็น 0 เสมอ ไม่ว่า totalIn/totalOut จะคำนวณออกมาเป็นเท่าไหร่ ป้องกันบิลที่ปิดแล้วโผล่กลับมาเป็นคงเหลือบวกได้อีกในทุกกรณี
**เพิ่มทางปลดล็อก**: เพราะการล็อกถาวรทำให้ "✏️ แก้ไขข้อมูลบิล" ใช้แก้ไขบิลที่ปิดไปแล้วไม่ได้ผลอีกต่อไป (คงเหลือจะโดนบังคับเป็น 0 เสมอแม้แก้จำนวนรับเข้า) — เพิ่ม checkbox "🔒 ปลดล็อกบิลนี้ (ยกเลิกสถานะปิดบิล)" ใน modal แก้ไขบิล โชว์เฉพาะบิลที่ `job.closed=true` เท่านั้น ต้องติ๊กก่อนถึงจะแก้ไขจำนวนให้มีผลจริง (ป้องกันปลดล็อกโดยไม่ตั้งใจ)

**ตอบคำถามเรื่อง v4/5/6 ไม่โชว์บิลที่ปิดแล้วในตัวเลือก "เลือกบิล"**: ยืนยันว่าเป็นจริงตามที่คาด — ตัวเลือกบิลในหน้าชุบ (plating_v4/5/6.html) ใช้ `calcJobRemainToPlate()` จาก `wip-calc.js` ซึ่งคำนวณจาก `job.qty` (ยอดรับเข้าที่บันทึกไว้จริง) โดยตรง **ไม่ใช่คนละระบบกับ job_master.html** — เพราะ `bulkCloseBill()` ตั้ง `job.qty` ให้เท่ากับ `totalOut` (มักเป็น 0 สำหรับบิลเก่าที่ไม่เคยออก INV) บิลที่ปิดแล้วจึงมี "เหลือให้ชุบ" เป็น 0 ในสูตรของ wip-calc.js ไปโดยอัตโนมัติเช่นกัน ไม่ต้องแก้โค้ดฝั่ง plating_v4/5/6 เพิ่มเติม — ใช้ข้อมูลต้นทางเดียวกัน (`job.qty`) จึงสอดคล้องกันเองอยู่แล้ว

## [job_master.html] แก้เลขผู้เสียภาษีลูกค้าหายจากใบ INV — ✅ เสร็จสมบูรณ์ (23 ก.ค. 2569)
ผู้ใช้พบว่า INV บางใบไม่มีเลขผู้เสียภาษีลูกค้าโชว์ ทั้งที่เพิ่มเลขผู้เสียภาษีในทะเบียนลูกค้าไว้ก่อนออก INV แล้ว (เลขผู้เสียภาษีในระบบนี้ไม่ได้เป็นช่องแยก — พิมพ์รวมเป็นบรรทัดสุดท้ายในกล่อง "ชื่อเต็ม/ที่อยู่" ของทะเบียนลูกค้า)

**บั๊กที่พบ**: ตอนเริ่มออก INV จากบิลใน Job Board มีโค้ด auto-fill ช่องลูกค้าในหัว INV — เดิมถ้าช่องลูกค้ายังว่างอยู่ จะเติมด้วย `job.customer` (ชื่อย่อสั้นๆ ที่บันทึกไว้ในบิล เช่น "CASH"/"SYS"/"GSC") **ตรงๆ โดยไม่ค้นทะเบียนลูกค้าแบบเต็มก่อนเลย** ถ้าเงื่อนไขนี้ถูกเข้า (ช่องลูกค้ายังว่าง) จะได้แค่ชื่อย่อ ไม่มีที่อยู่/เลขผู้เสียภาษีเต็มติดไปด้วย

**แก้แล้ว**: auto-fill ตอนนี้ค้นทะเบียนลูกค้า (`customers[]`) ก่อนเสมอ โดยจับคู่จาก "ชื่อย่อ" (`customers[].name`) กับ `job.customer` — ถ้าเจอในทะเบียน ใช้ `addressText` แบบเต็ม (รวมเลขผู้เสียภาษี) แทน ถ้าไม่เจอ (ลูกค้าใหม่ที่ยังไม่ลงทะเบียน) ค่อย fallback เป็นชื่อย่อดิบเหมือนเดิม

**เพิ่มเติม — ตัวช่วยเช็คต้นเหตุอีกทาง**: เพิ่มการเตือน "⚠️ ซ้ำ" ในหน้า "ทะเบียนลูกค้าทั้งหมด" ถ้ามีลูกค้า**ชื่อย่อซ้ำกัน**มากกว่า 1 รายการ (แถวจะไฮไลต์พื้นแดงอ่อน) เพราะถ้ามีชื่อย่อซ้ำ (เช่น มีรายการเก่าที่ยังไม่มีเลขผู้เสียภาษี ซ้ำกับรายการใหม่ที่เพิ่งเติมเลขผู้เสียภาษีแล้ว) ระบบ auto-fill จะจับคู่ได้แค่ตัวแรกที่เจอในอาเรย์เท่านั้น อาจได้ข้อมูลเก่า/ผิดตัวได้ — **แนะนำให้เช็คทะเบียนลูกค้าว่ามี "บริษัท ซี.เอ.เอส.เอช โอโตพาร์ทส" ซ้ำกัน 2 รายการไหม ถ้ามีให้ลบตัวเก่าที่ไม่มีเลขผู้เสียภาษีทิ้ง เก็บไว้แค่ตัวที่ข้อมูลครบ**

## [job_master.html] พิมพ์ซ้ำบิลเก่าดึงข้อมูลลูกค้าล่าสุดจากทะเบียนเสมอ — ✅ เสร็จสมบูรณ์ (23 ก.ค. 2569)
หลังแก้บั๊กก่อนหน้า ผู้ใช้ทดสอบเปิด**บิลเก่าที่เคยออกไปแล้ว**เพื่อพิมพ์ซ้ำ พบว่ายังไม่โชว์เลขผู้เสียภาษี ทั้งที่แก้ทะเบียนลูกค้าให้ถูกแล้ว — ยืนยันสาเหตุจริง: `inv.customer` เป็นก้อนข้อความที่ **แช่แข็งไว้ตั้งแต่ตอนออกบิลครั้งแรก** ไม่ได้ดึงสดจากทะเบียนลูกค้าทุกครั้งที่พิมพ์ซ้ำ (ตั้งใจออกแบบไว้แบบนี้แต่แรก เพื่อรักษาความถูกต้องของเอกสารย้อนหลัง) — ถามผู้ใช้แล้วว่าต้องการให้พิมพ์ซ้ำดึงข้อมูลล่าสุดจากทะเบียนเสมอ (แม้เป็นบิลเก่า) แทนพฤติกรรมเดิม

**แก้แล้วตามที่ต้องการ**: ทั้ง `printInvoice()` (ใบกำกับภาษี/ใบส่งสินค้าแบบเต็มหน้า) และ `buildFormOverlayBody()` (ใช้โดย `printInvoiceOnForm()` — พิมพ์ทับฟอร์มสำเร็จรูป) ตอนนี้จะ**ค้นทะเบียนลูกค้าสดทุกครั้งที่พิมพ์** โดยจับคู่จาก**บรรทัดแรกของชื่อลูกค้า** (ปกติคือชื่อบริษัท) ในข้อความที่บันทึกไว้กับ INV เทียบกับบรรทัดแรกของ `addressText` ในทะเบียน — ถ้าเจอ ใช้ข้อมูลล่าสุดจากทะเบียนแทนของแช่แข็งเดิมทันที ถ้าหาไม่เจอ (เช่น ลูกค้าถูกลบออกจากทะเบียนไปแล้ว) ค่อย fallback ไปใช้ `inv.customer` เดิมเหมือนก่อนแก้
**ข้อควรทราบ**: `inv.customer` ที่บันทึกไว้จริงในฐานข้อมูลไม่ได้ถูกแก้ไข/เขียนทับ — จุดนี้แก้แค่ตอน "แสดงผลตอนพิมพ์" เท่านั้น ถ้าลบ fix นี้ออกในอนาคต ข้อมูลเดิมจะยังอยู่ครบ ไม่มีอะไรสูญหาย

## [job_master.html] เพิ่มตำแหน่งบรรทัดที่ 5 ในโหมด "พิมพ์ทับฟอร์มสำเร็จรูป" — ✅ เสร็จสมบูรณ์ แต่ยังไม่ผ่านการทาบกับฟอร์มจริง (23 ก.ค. 2569)
หลังแก้เรื่องดึงข้อมูลลูกค้าล่าสุดแล้ว ผู้ใช้แจ้งว่าโหมด "พิมพ์ทับฟอร์มสำเร็จรูป" (`printInvoiceOnForm`/`buildFormOverlayBody`) ยังไม่โชว์เลขผู้เสียภาษีเหมือนเดิม ทั้งที่โหมดพิมพ์เต็มหน้า (`printInvoice`) โชว์ถูกแล้ว

**สาเหตุ**: โหมดพิมพ์ทับฟอร์มใช้ตำแหน่ง x,y ตายตัว (`FORM_POS.custLines`) วางทับกระดาษที่พิมพ์ล่วงหน้าไว้แล้ว เดิมมีแค่ **4 ตำแหน่ง** (`[58, 63.5, 69, 76]`) แต่ที่อยู่ลูกค้าเต็มตอนนี้มี **5 บรรทัด** (ชื่อ/ที่อยู่ถนน/เขต-จังหวัด/โทร/เลขผู้เสียภาษี) — บรรทัดที่ 5 (เลขผู้เสียภาษี) เกินตำแหน่งที่มี จึงถูกตัดทิ้งไปเงียบๆ ที่ `if (i < P.custLines.length)`
**พบข้อมูลเพิ่มเติมระหว่างแก้**: จาก `printCalibrationGrid()` (ฟังก์ชันพิมพ์กริดทดสอบ) มี label กำกับไว้ว่า `P.custLines[3]` (ตำแหน่งที่ 76mm) ถูกออกแบบไว้ให้เป็น **"เลขผู้เสียภาษี"** มาตั้งแต่แรก — สันนิษฐานว่าฟอร์มเดิมออกแบบไว้สำหรับ 4 บรรทัดพอดี (ไม่รวมเบอร์โทร) แต่ภายหลังมีการเพิ่มบรรทัดเบอร์โทรเข้าไปในที่อยู่ลูกค้า ดันตำแหน่งเลขผู้เสียภาษีให้ตกไปเป็นบรรทัดที่ 5 แทน

**แก้แล้วตามที่ผู้ใช้เลือก**: เพิ่มตำแหน่งที่ 5 (82.5mm) ใน `custLines` — **ประมาณค่าจากช่วงห่างเดิม (5.5-7mm ต่อบรรทัด) เท่านั้น ยังไม่ได้ทาบกับกระดาษฟอร์มจริง** อาจเพี้ยนไปทับข้อความอื่นบนฟอร์มได้ — **รอผู้ใช้พิมพ์ทดสอบแล้วแจ้งกลับว่าตำแหน่งพอดีไหม** ถ้าไม่พอดี ใช้ปุ่ม "พิมพ์กริดทดสอบ" ในระบบทาบกับฟอร์มเปล่าหาค่า mm ที่ถูกต้อง แล้วแจ้งพิกัดมาปรับให้ใหม่ได้

**23 ก.ค. 2569 (ยืนยันผลจริง)**: ผู้ใช้พิมพ์ทับฟอร์มจริงแล้วถ่ายรูปมายืนยัน — ตำแหน่งบรรทัดที่ 5 (เลขผู้เสียภาษี) ที่ 82.5mm **พอดีตั้งแต่ครั้งแรก ไม่ต้องปรับ** ✅ มีจุดเดียวที่ต้องขยับเพิ่ม: บรรทัดแรก (ชื่อบริษัท) ทับกับ label "ชื่อลูกค้า / Customer Name" ที่พิมพ์ไว้บนฟอร์มอยู่เล็กน้อย — ขยับตำแหน่งที่ 1 ขึ้น จาก 58mm เป็น 56.5mm (บรรทัด 2-5 ไม่แตะ เพราะตำแหน่งดีอยู่แล้ว) `custLines` ปัจจุบัน: `[56.5, 63.5, 69, 76, 82.5]`

---

## 📌 แผนงานที่ตกลงกันไว้ (23 ก.ค. 2569)
**ลำดับแก้ปัญหาข้อมูลขัดแย้ง: เริ่มจาก job_master.html (jm) ก่อน แล้วค่อยสรุปแก้ bl_stock.html (bl) ทีหลัง**
- เหตุผล: ปัญหา "ข้อมูลขัดแย้ง" ที่เห็นใน bl_stock.html (WIP+Delivery เกินยอดรับเข้า, banner เตือนสีแดงที่เพิ่งทำ) กับปัญหาบิลเก่าค้างใน job_master.html (เช่น P/No 7G-3101-12 ที่มีบิลค้าง "จ่ายออก=0" เกินกำหนดหลายสิบวัน) **คาดว่าเป็นสาเหตุเดียวกัน** เพราะ bl_stock.html ดึง "รับเข้า" มาจาก jm โดยตรง
- ขั้นตอน: (1) ใช้ปุ่ม "🧹 ปิดบิลเก่าเป็นชุด" ใน job_master.html ไล่เคลียร์บิลเก่าที่ใช้หมดจริงแล้วแต่ยังโชว์คงเหลือค้าง ทีละ P/No หรือทั้งระบบ → (2) กลับมาเช็ค bl_stock.html อีกครั้งว่า banner แจ้งเตือนข้อมูลขัดแย้งลดลง/หายไปหรือไม่ → (3) ถ้ายังเหลือ warning อยู่ ค่อยไล่ดูเป็นกรณีๆ ว่าเป็นปัญหาอื่น (เช่น จับคู่ P/No ระหว่าง jm กับข้อมูลชุบ v4/5/6 ไม่ตรงกัน) ไม่ใช่ปัญหาบิลเก่าที่เคลียร์ไปแล้ว
- **สถานะ**: ยังไม่เริ่มไล่เคลียร์จริง — รอผู้ใช้ทดสอบเครื่องมือ Bulk Close ก่อน

## [chem_master.html, po_master.html] Supplier Evaluation (Sup EVA) — ✅ เสร็จสมบูรณ์ (25 ก.ค. 2569)
สร้างระบบประเมินผู้ขาย (Supplier Evaluation Report) ตามฟอร์ม Excel ต้นฉบับ (รหัสเอกสาร **APK-FR-PU-004**) โดยดึงข้อมูลอัตโนมัติจาก chem_master.html แทนการกรอกมือทั้งหมด — เดิมเช็คแล้วพบว่า `po_master.html` (ระบบออก PO อย่างเดียว) ไม่มีข้อมูลรองรับหัวข้อประเมินเลยแม้แต่ข้อเดียว แต่ `chem_master.html` มีระบบ QC ตอนรับเข้าเคมีอยู่แล้วที่ครอบคลุม 3 ใน 7 หัวข้อพอดี (Certificate, เอกสารส่งของ, บรรจุภัณฑ์) จึงออกแบบให้เชื่อมสองระบบเข้าด้วยกัน

**[chem_master.html] เพิ่มการผูก PO + คำนวณ "จัดส่งตรงเวลา" อัตโนมัติ**
- เพิ่ม dropdown **"อ้างอิง PO"** ในฟอร์มบันทึกรับเข้าเคมี — ดึงรายการ PO ของผู้ขายนั้นมาจาก `po_master.html` (อ่านอย่างเดียวผ่าน Firebase, cache ไว้ใน `poListCache`/`poVendorsCache`, โหลดผ่าน `loadPoCache()`) แทนการพิมพ์เลข PO เองในช่อง "bill" แบบเดิม (ยังคงช่องพิมพ์มือไว้เป็น fallback ถ้าไม่เจอ PO ใน dropdown)
- เพิ่มฟังก์ชัน `calcOnTimeResult()`/`renderOnTimeAuto()` — เทียบวันที่รับ (`inDate`) กับ `dueDate` ของ PO ที่ผูกไว้ โชว์ผล ✅ตรงเวลา/❌ล่าช้า **แบบสด** ก่อนกดบันทึก (ไม่ใช่หัวข้อ QC แบบกดเลือกเอง เพราะคำนวณจากข้อมูลจริง)
- บันทึกผลลงในทุก record รับเข้า: `linkedPoId`, `linkedPoNo`, `linkedPoDueDate`, `onTimeResult` ('pass'/'fail'/'')
- **ไม่ได้เพิ่ม field ใหม่ตามที่คุยกันไว้ตอนแรก** ("จัดส่งตรงตามความต้องการ" เช็คบอกเดียว) เพราะคุยกันแล้วว่าซ้ำซ้อนกับ 3 ใน 5 หัวข้อ QC เดิมที่มีอยู่แล้ว (`qcTopics`: สภาพบรรจุภัณฑ์, สีและลักษณะทางกายภาพ, กลิ่น, ปริมาณตรงตามใบส่งของ, มี Certificate แนบ) — ใช้ของเดิมแทน เพิ่มแค่ "จัดส่งตรงเวลา" เป็นเรื่องเดียวที่ไม่มีข้อมูลรองรับมาก่อน

**[po_master.html] เพิ่มแท็บ "📝 Sup EVA"**
- อ่านข้อมูลจาก chem_master.html (อ่านอย่างเดียว ผ่าน `CHEM_FB_PATH`) มาคำนวณคะแนนอัตโนมัติ 5 ใน 7 หัวข้อ ตามกฎ **"ทุกครั้งที่เกิดปัญหา ลด 5 คะแนน"** ที่มีอยู่แล้วในฟอร์ม Excel เดิม:
  1. การเคลมวัตถุดิบ (50) — นับจาก `qcResult==='fail'` โดยรวมของแต่ละครั้งรับเข้า
  2. ใบรับรองวัตถุดิบ (30) — นับจากหัวข้อ QC ที่ชื่อมีคำว่า "Certificate"
  3. เอกสารการส่งสินค้า (10) — นับจากหัวข้อ QC ที่ชื่อมีคำว่า "ใบส่งของ"
  4. ลักษณะของบรรจุภัณฑ์ (20) — นับจากหัวข้อ QC ที่ชื่อมีคำว่า "บรรจุภัณฑ์"
  5. จัดส่งตรงตามกำหนดเวลา (50) — นับจาก `onTimeResult` ที่คำนวณใน chem_master.html
  - รายการรับเข้าที่ไม่มีข้อมูลหัวข้อนั้น (เช่น ไม่ได้ผูก PO เลยไม่มี `onTimeResult`) **ไม่ถูกนับ** ไม่ตัดคะแนนเดา
- 2 หัวข้อที่ยังไม่มีข้อมูลรองรับ (การตอบกลับใบรายงานปัญหา, การขนส่งรอบพิเศษ) เป็นช่องกรอกคะแนนเอง (default เต็ม 20 เหมือนพฤติกรรมเดิมของฟอร์ม Excel) เก็บไว้ใน `supEvaManual` (persist ผ่าน Firebase คู่กับ `poList`/`vendors` เดิม ไม่ทับข้อมูลเก่า)
- ตาราง "ประเด็นที่เป็นปัญหา" รวมรายการที่มา 2 ทาง: auto-suggest จากรายการที่ `qcResult==='fail'` (พร้อมเหตุผลจาก `qcFailReason`) + เพิ่มเองด้วยมือได้ (เก็บใน `supEvaManual`)
- กราฟแท่งรายเดือน (Jan-Dec) คำนวณคะแนนทั้ง 12 เดือนของปีที่เลือกให้เห็นแนวโน้ม ไม่ใช่แค่เดือนที่เลือกดู
- เกรด A-D (ด้านส่งมอบเท่านั้น ตรงกับฟอร์มต้นฉบับที่มีเกรดแค่ฝั่งนี้) คำนวณจาก `seGrade()`: A=91-100, B=81-90, C=71-80, D=0-70
- ปุ่ม "🖨️ พิมพ์รายงาน" เปิดหน้าต่างใหม่จัดหน้าสำหรับพิมพ์ พร้อมรหัสเอกสาร APK-FR-PU-004 ที่ท้ายเอกสาร ตรงกับต้นฉบับ
- เพิ่ม `TAB_IDS`/`tab()` รองรับแท็บใหม่ (ต้องไปเพิ่มสิทธิ์การมองเห็นแท็บนี้ให้ role ที่เกี่ยวข้องใน auth_admin.html เองด้วย ถ้าใช้ role-based tab hiding)

**ข้อจำกัดที่ควรทราบ**: การจับคู่ผู้ขายใช้ `chemItem.supplier === vendor.name` (string ตรงตัวเป๊ะ) — ถ้าชื่อผู้ขายใน chem_master สะกดต่างจากชื่อในทะเบียนผู้ขาย po_master (เช่น เว้นวรรค/ย่อไม่ตรงกัน) จะจับคู่ไม่เจอ ข้อมูลจะไม่ถูกดึงมาคำนวณ — ควรเช็คว่าเขียนตรงกันทั้งสองระบบ (ยังไม่ได้ใส่ normalize แบบ `chemNameKey()` ที่ทำไว้ให้ job_master.html ก่อนหน้านี้ — ถ้าเจอปัญหาแบบเดียวกัน แจ้งได้ จะใส่ normalize ให้เพิ่ม)

**25 ก.ค. 2569 (แก้ตามข้อเสนอผู้ใช้)**: เปลี่ยน "จัดส่งตรงเวลา" จากคำนวณอัตโนมัติ (เทียบวันที่รับกับ `dueDate` ของ PO) เป็น**หัวข้อ QC แบบกดเลือกเองที่ 6** (`qcTopics`) เหมือนอีก 5 หัวข้อ — เหตุผล: PO ที่สร้างจากระบบ Auto-Reorder บางใบยังไม่มี `dueDate` (รอผู้อนุมัติกรอก) หรือของมาถึงจริงก่อนได้รับอนุมัติ เทียบวันที่อัตโนมัติแล้วเสี่ยงตัดสินผิดได้ ให้คนตัดสินเองแม่นยำกว่า
- Dropdown "อ้างอิง PO" **ยังคงอยู่** — เปลี่ยนบทบาทจาก "ตัวตัดสินอัตโนมัติ" เป็น "ข้อมูลอ้างอิงประกอบการตัดสินใจ" เท่านั้น (โชว์กำหนดส่งของ PO ที่เลือกเป็น hint ข้างช่องวันที่รับ ผ่าน `renderOnTimeHint()`) ยังผูก `linkedPoId`/`linkedPoNo`/`linkedPoDueDate` ไว้ในแต่ละ record เพื่อสอบกลับย้อนหลังได้ แต่ไม่ใช้ตัดสินคะแนนอัตโนมัติแล้ว
- ลบฟังก์ชัน `calcOnTimeResult()`/`renderOnTimeAuto()`/field `onTimeResult` ออก — แทนที่ด้วย `renderOnTimeHint()`
- [po_master.html] `seCalcMonth()` เปลี่ยนมาอ่านคะแนน "จัดส่งตรงเวลา" จาก `qcTopicResults` (ผ่าน `seFindTopicResult(r,'จัดส่งตรงเวลา')`) แทน `r.onTimeResult` — สอดคล้องกับหัวข้ออื่นๆ ที่ดึงจาก QC checklist เหมือนกันหมดแล้วตอนนี้ (Certificate/เอกสารส่งของ/บรรจุภัณฑ์/จัดส่งตรงเวลา ทั้ง 4 หัวข้อใช้กลไกเดียวกัน)

---

## ⏳ รายการที่รู้ว่ายังค้างอยู่ (ยังไม่เสร็จ/รอตัดสินใจ)
- Firebase security rules: B2 กับ R1 project ยัง**เปิดกว้างอยู่** (ไม่ได้ล็อกเหมือน B1) — ต้องยืนยันว่า fb-auth.js อัปเดตครบทั้ง 18 ไฟล์และเทสต์แล้วหรือยัง
- ตาราง Backlog/Auto Scheduler ใน production_order.html — รอตัดสินใจว่าจะลบ UI ทิ้งหรือเก็บไว้
- Sup EVA: การจับคู่ชื่อผู้ขายระหว่าง chem_master กับ po_master ยังเป็น exact string match — เสี่ยงจับคู่พลาดถ้าสะกดไม่ตรงกัน (ดูรายละเอียดด้านบน)
