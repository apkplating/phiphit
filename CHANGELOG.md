# CHANGELOG — APK Plating Web Suite

> **วิธีใช้ไฟล์นี้**
> - แนบไฟล์นี้มาด้วยทุกครั้งที่เปิดแชทใหม่กับ Claude เพื่อขอแก้โค้ด (พร้อมกับไฟล์ .html ที่จะแก้)
> - ทุกอย่างที่ทำเครื่องหมาย **✅ เสร็จสมบูรณ์** คือจุดที่ตรวจสอบแล้วว่าใช้งานได้ตามต้องการ — ห้ามแก้/ลบ/เปลี่ยน behavior โดยไม่ได้รับอนุญาตชัดเจน
> - เมื่อ Claude แก้ฟีเจอร์ใหม่เสร็จและทดสอบแล้วว่าใช้ได้ ให้เพิ่มบรรทัดใหม่ต่อท้ายไฟล์นี้ (อย่าลบของเก่า) พร้อมวันที่และไฟล์ที่แก้
> - ถ้าเจอว่าโค้ดจริงกับ CHANGELOG ไม่ตรงกัน (ฟีเจอร์ที่บอกว่าเสร็จแล้วแต่หายไปจากโค้ด) ให้หยุดถามผู้ใช้ก่อน อย่าเดาเอง

---

## รูปแบบบันทึกแต่ละรายการ
```
### [ไฟล์] ชื่อฟีเจอร์/สิ่งที่แก้ — วันที่ (สถานะ)
รายละเอียดสั้นๆ ว่าทำอะไร ตรรกะสำคัญคืออะไร จุดที่ต้องระวังถ้าจะแก้ต่อ
```

---

## Core Architecture (คงที่มาตั้งแต่เริ่มต้น)
- Static HTML + vanilla JS โฮสต์บน GitHub Pages — repo: `apkplating/phiphit`
- Firebase Realtime Database แยกตามสาย: primary `platingapp-92e21-c1346` (ใช้ร่วมกับ B1), `platingapp-b2` (B2), `platingapp-r1` (R1)
- ระบบสิทธิ์กลาง: `auth-central.js` (CentralAuth module), เก็บ role matrix ที่ `auth_admin.html`, Firebase path `/auth-central.json`
- ฟอนต์ IBM Plex Sans Thai, ธีมสี navy/steel
- แนวทางแก้โค้ด: **surgical minimal diff** ไม่ rewrite ทั้งไฟล์, เช็ค syntax ด้วย Node.js ก่อนส่งมอบเสมอ, เช็ค div/table tag balance ด้วย Python, ไฟล์ส่งมอบไปที่ `/mnt/user-data/outputs/`

---

## [wip-calc.js] Shared WIP calculation module — ✅ เสร็จสมบูรณ์
โมดูลกลางอ่านค่าจาก `dayRecords` ตรงๆ ในทุกโปรเจกต์ Firebase (B1/B2/R1) เพื่อให้ยอดสต็อกคงเหลือตรงกันทุกที่ที่เรียกใช้ (bl_stock.html, production_order.html, plating_v4/5/6)

## [plating_v4/v5/v6, bl_stock.html] Bill overage & NOBILL — ✅ เสร็จสมบูรณ์
- **Hard-block** เมื่อกรอกจำนวนเกินยอดบิล (ป้องกันตัดสต็อกเกิน)
- **NOBILL ต้องขออนุมัติ** ก่อนตัดสต็อก ไม่ silent-deduct แบบเดิม — มีระบบสิทธิ์อนุมัติละเอียด (`auth-central.js` / `auth_admin.html`)
- หลักการ: overage = บล็อกทันที, NOBILL/ไม่มีสต็อก = ต้องอนุมัติก่อน (ไม่ใช่บล็อกเฉยๆ)

## [job_master.html, bl_stock.html] Firebase PUT vs PATCH bug — ✅ เสร็จสมบูรณ์ (แก้ถาวรแล้ว)
บั๊กร้ายแรง: ใช้ PUT แทน PATCH ทำให้ข้อมูล invoice/customer/priceList ถูกเขียนทับหายไปทั้ง field เพราะ race condition — แก้ด้วย flag `_hasSuccessfulCloudSync` และ localStorage backup guard ใน job_master.html **ห้ามเปลี่ยนกลับไปใช้ PUT ตรงจุดนี้เด็ดขาด**

## [bl_stock.html] STOCK1/STOCK2/STOCK3 traceability — ✅ เสร็จสมบูรณ์
แก้บั๊กหลายชั้น: นับ delivery ซ้ำสอง, wipMap ตัดข้อมูล 30 วันทำให้ STOCK2 เป็นศูนย์ผิด, partName key ไม่ตรงกัน (bare vs concatenated), NOBILL โดนรวมเข้าไปในยอด plated ผิด — เพิ่ม live WIP ผ่าน live.json และ verify bar

## [job_master.html] ใบส่งคืนงานเคลม (Claim Return Document) — ✅ เสร็จสมบูรณ์
- ระบบเลขที่เอกสาร prefix `CL`
- เลือกลูกค้าก่อนในโหมดเคลม, template พิมพ์แยกเฉพาะ
- **23 ก.ค. 2569**: ปรับขนาดกระดาษพิมพ์เป็นกระดาษต่อเนื่อง 9x5.5 นิ้ว (`@page{size:9in 5.5in}`), ลด blank rows จาก 6 เหลือ 3 แถวให้พอดีหน้า
- **23 ก.ค. 2569**: คอลัมน์ "จำนวน" แสดง `<จำนวนชิ้น> ชิ้น (<น้ำหนัก กก.>)` โดยคำนวณน้ำหนักจาก Part Master (`pmPart.weightPerPc` กรัม/ชิ้น × จำนวนชิ้น ÷ 1000) ไม่ใช้ `l.qty` ตรงๆ (เพราะอาจไม่ตรงกับน้ำหนัก/ชิ้นจริงปัจจุบันใน PM) — fallback ไป `l.qty` ถ้า PM ไม่มีข้อมูลน้ำหนัก — ใช้ฟังก์ชัน `invLineQtyDisplay()` ร่วมกับ `printInvoice()`/`printInvoiceOnForm()` ด้วย

## [production_order.html] BL Stock → เข้าคิว → ใบสั่งผลิต — ✅ เสร็จสมบูรณ์ (23 ก.ค. 2569)
- เปลี่ยน behavior ปุ่ม "➕ เข้าคิว" ในตาราง "รายการรอชุบจาก BL Stock": **เดิม** ดันเข้า Backlog แล้วรอ Auto Scheduler (05:00 หรือกดรันเอง) ตัดกำลังการผลิตก่อนออก PO → **ใหม่** สร้างใบสั่งผลิต (PO) ให้ทันทีที่กด ไม่เช็คกำลังการผลิตสูงสุด (เพราะผู้ใช้กดยืนยันเองทีละแถว/บิลอยู่แล้ว) โผล่ที่ตาราง "📜 ใบสั่งผลิตทั้งหมด" ทันที
- รายการที่ยังชุบไม่ครบยอดจะกลับมาโชว์ในตาราง BL Pending เองอัตโนมัติ (คำนวณสดจากข้อมูลจริงทุกครั้งที่กด "ดึงข้อมูลจาก BL Stock" — ไม่ต้องเขียนโค้ดเพิ่ม เพราะระบบ compute ใหม่จากล็อต vs WIP จริงเสมอ)
- **หมายเหตุค้างอยู่**: ตาราง "🗂️ คิวรอผลิต (Backlog)" กับปุ่ม "▶ รันจัดคิวเดี๋ยวนี้" ยังไม่ได้ลบออกจากหน้า เพราะไม่แน่ใจว่า GitHub Actions script (รันทุกวัน 05:00) ยังพึ่งพา `production-backlog.json` อยู่หรือไม่ — ตอนนี้ตารางนี้จะว่างเปล่าเสมอเพราะไม่มีอะไรป้อนเข้าไปแล้ว ยังไม่ตัดสินใจว่าจะลบทิ้งหรือไม่

## [plating_v4.html] Chemical Movement Table (Auto/Manual/AH/Month filter) — ✅ เสร็จสมบูรณ์ (23 ก.ค. 2569)
ตาราง "📋 รายการเคลื่อนไหว" ในแท็บเคมี:
- เดิมโชว์แค่ยอดเบิกแบบ Auto (คำนวณจาก AH) เท่านั้น — รายการเบิก Manual (เติมเคมีพิเศษ, เก็บใน `chemRecords` type `out`) ไม่เคยโผล่ในตารางนี้เลย (มีแค่รวมยอดในสรุปด้านบน)
- **แก้แล้ว**: ดึง Auto (จาก `dayRecords`) + Manual (จาก `chemRecords`) มารวมกัน เรียงตามวันที่จริง แยกคอลัมน์ "เบิก Auto (ลิตร)" กับ "เบิก Manual (ลิตร)" ชัดเจน ไม่ปนกัน
- เพิ่มคอลัมน์ **AH** (โชว์ค่ากระแสไฟที่ใช้คำนวณยอด Auto วันนั้น)
- เพิ่ม **ตัวกรองเดือน** (dropdown สร้าง option อัตโนมัติจากเดือนที่มีข้อมูลจริง) — running balance ยังคำนวณจากประวัติทั้งหมดเสมอ ไม่ได้ reset ตามเดือนที่กรอง
- เพิ่ม **แถวรวมท้ายตาราง** สรุปยอดรับเข้า/AH รวม/เบิก Auto รวม/เบิก Manual รวม/คงเหลือล่าสุด ของช่วงที่กรองอยู่
- **23 ก.ค. 2569 (เพิ่มเติม)**: แก้บั๊กตัวกรองเดือน — popup ของ `<select id="chemMonthFilter">` ตอนกดเปิดดูตัวเลือก ตัวหนังสือกลายเป็นสีขาวบนพื้นขาว (มองไม่เห็น) เพราะ `<option>` inherit สี `color:#fff` จาก select ที่ตั้งใจไว้สำหรับตอนปิด (แสดงบนแถบ navy) — แก้ด้วย CSS rule `#chemMonthFilter option{color:#0f172a;background:#fff;}` แยกสีของ popup ออกจากกล่องตอนปิดอย่างชัดเจน **สำคัญ:** rule นี้ผูกกับ `id="chemMonthFilter"` — ถ้าจะเพิ่ม dropdown อื่นที่ใช้ธีมเดียวกัน (พื้นเข้ม/ตัวหนังสือขาวตอนปิด) ต้องเพิ่ม CSS rule แยกสำหรับ id นั้นด้วย ไม่ได้ apply อัตโนมัติกับ select ทุกตัว

## [plating_v5_B2.html, plating_v6_R1.html] พอร์ต Chemical Movement Table จาก v4 — ✅ เสร็จสมบูรณ์ (23 ก.ค. 2569)
พอร์ตฟีเจอร์เดียวกับ v4 (Auto/Manual แยกคอลัมน์, AH, ตัวกรองเดือน, แถวรวมท้ายตาราง, แก้สี dropdown popup) มาให้ v5 (B2) และ v6 (R1) ครบทั้งคู่ — ปรับให้เข้ากับโครงสร้างเดิมของแต่ละไฟล์ที่ต่างจาก v4 เล็กน้อย:
- v5/v6 ฝั่ง "รับเข้า" คือ **จัดซื้อโดยตรง** (`chemRecords` type `in`) ไม่ใช่เบิกจาก Master Stock เหมือน v4 — โครงสร้างคอลัมน์ฝั่งนี้ (เลขที่ใบรับงาน, รับเข้า(ถัง), ผู้บันทึก) คงไว้ตามเดิม ไม่ได้แตะ
- `calcDayChemUsedLiter()` ของ v5/v6 มี logic กรองตาม **บ่อ (batch)** ที่ v4 ไม่มี — คงไว้ตามเดิม ไม่ได้เปลี่ยน
- โครงสร้างข้อมูล manual dosing (`chemRecords` type `out`, `isManual:true`) เหมือนกันทั้ง 3 ไฟล์ ใช้ logic รวม Auto+Manual แบบเดียวกันได้เลย

## [plating_v4.html, plating_v5_B2.html, plating_v6_R1.html] แก้คอลัมน์ "AH" ให้ถูกต้องตามประเภทการคำนวณของแต่ละเคมี — ✅ เสร็จสมบูรณ์ (23 ก.ค. 2569)
บั๊ก: คอลัมน์ "AH" ในตาราง "รายการเคลื่อนไหว" เดิมโชว์ค่า AH ของวันนั้นให้ **ทุกแถว Auto เหมือนกันหมด** ทั้งที่เคมีบางตัว (เช่น ALX-441A ในกลุ่ม Black Chromate) จริงๆ คำนวณยอดใช้จาก **จำนวนโม่ต่อสี** (K/B/C) ไม่ใช่ AH — ทำให้ตัวเลขที่โชว์ไม่ตรงกับที่ใช้คำนวณจริง สร้างความสับสน
- **แก้แล้ว**: เพิ่มฟังก์ชัน `calcDayChemRef(day)` คู่กับ `calcDayChemUsedLiter(day)` เดิม — เช็คจาก `chemSettings` ว่าเคมีตัวนี้ผูกกับ group/sub-chemical แบบ `type:'AH'` หรือ `type:'COLOR'` แล้วคืนค่าอ้างอิงที่ตรงกับสูตรคำนวณจริงของเคมีนั้น (AH หรือจำนวนโม่ของสีที่ตรงกัน)
- คอลัมน์เปลี่ยนชื่อเป็น **"AH/โม่"** และแสดงผลต่างกันตามประเภท: เคมีที่คิดจาก AH → โชว์ "<เลข> AH", เคมีที่คิดจากโม่ → โชว์ "<เลข> โม่ (<สี>)" เช่น "5 โม่ (K ดำ)"
- แถวรวมท้ายตาราง ("AH รวม") **สรุปเฉพาะแถวที่เป็น AH-type จริงเท่านั้น** ไม่รวมยอดโม่เข้าไปด้วย (หน่วยต่างกัน รวมกันแล้วความหมายผิด)
- **23 ก.ค. 2569 (เพิ่มเติม)**: ตอนเพิ่มแถวรวมครั้งแรก ลืมรวมยอดสำหรับเคมีกลุ่ม COLOR-type (ที่คิดจากจำนวนโม่) ทำให้ช่องนี้ว่างเปล่าเมื่อรายการทั้งหมดในเดือนนั้นเป็นแบบโม่ (ไม่มี AH เลย) — แก้เพิ่ม `sumMolds`/`sumMoldsColor` แยกจาก `sumAH` เพื่อสรุปยอดโม่รวมของสีนั้นด้วย (เคมีตัวหนึ่งมีสีเดียวคงที่ทั้งตาราง จึงรวมกันได้ตรงความหมาย ไม่ปนข้ามสี)
- v5/v6 (มี concept "บ่อ/batch") ใช้ logic เดียวกับ `calcDayChemUsedLiter` เดิมที่กรองตาม `item.batches` ก่อนนับจำนวนโม่ ให้ตรงกับสูตรคำนวณจริงของแต่ละไฟล์

## [plating_v4.html, plating_v5_B2.html, plating_v6_R1.html] 🔴 แก้บั๊กใหญ่: การจับคู่ "สี" ของเคมีกลุ่ม COLOR ผิดมาตั้งแต่ต้น — ✅ เสร็จสมบูรณ์ (23 ก.ค. 2569)
**นี่คือบั๊กที่มีมาก่อนหน้าเซสชันนี้แล้ว ไม่ใช่บั๊กที่เพิ่งเกิดจากการแก้ล่าสุด** — พบระหว่างตรวจสอบตามที่ผู้ใช้ทักท้วงว่าคอลัมน์ "AH/โม่" ของ ALX-441A ควรโชว์ "(K)" ไม่ใช่ "(ทุกสี)"

**สาเหตุที่แท้จริง**: ตามโครงสร้างข้อมูลจริงของ `chemSettings` (ดู `saveChemSettings()`) — `{ name, type:'AH'|'COLOR', color:'B'|'C'|'K', chemicals:[{name, rate}] }` — **สีถูกเก็บไว้ที่ระดับกลุ่ม (`c.color`) ค่าเดียวใช้ร่วมกันทั้งกลุ่ม** (เลือกผ่าน radio "ใช้กับสี" ตอนตั้งค่า) ไม่ได้เก็บแยกรายสารย่อย (`ch.color` **ไม่มีอยู่จริง** ในโครงสร้างข้อมูล เป็น `undefined` เสมอ)

แต่ทุกฟังก์ชันที่คำนวณยอดใช้เคมีของกลุ่ม COLOR กลับไปอ่าน `ch.color` (สารย่อย) แทนที่จะเป็น `c.color` (กลุ่ม) — ทำให้ `color` เป็นค่าว่างเสมอ แล้วตกไปที่ branch `else` ซึ่ง **รวมจำนวนโม่ทุกสี (B+C+K) เข้าด้วยกัน** ทั้งที่ควรใช้แค่จำนวนโม่ของสีที่กลุ่มนั้นตั้งไว้เท่านั้น ผลคือ **ยอดใช้เคมีของทุกกลุ่ม COLOR-type ที่มีสารย่อยถูกคำนวณสูงเกินจริง** (สูงกว่าที่ควรเป็น ~2-3 เท่า ถ้าโม่กระจายทั้ง 3 สี) ตลอดมา

**จุดที่แก้ (5 ฟังก์ชัน × 3 ไฟล์ = 15 จุด)**:
1. `calcAutoChemUsed()` — ยอด autoOut ที่ใช้แสดงในสรุปด้านบนการ์ด (คงเหลือ/เบิกจากระบบ) → เปลี่ยนเป็น `matchedGroup.color`
2. `calcDayChemUsedLiter()` — ยอดเบิกรายวันในตาราง "รายการเคลื่อนไหว" (ที่ใช้คำนวณ running balance ด้วย) → เปลี่ยนเป็น `c.color`
3. `calcDayChemRef()` — ฟังก์ชันใหม่ที่เพิ่งเพิ่มไปเมื่อกี้ (คอลัมน์ AH/โม่) → เปลี่ยนเป็น `c.color`
4. `calcChemMonthTotal()` — ยอดรวมเคมีสะสมรายเดือน (การ์ด "สรุปเคมีสะสมทั้งหมด") → เปลี่ยนเป็น `c.color`
5. `calcAutoChemUsed_forDay()` — ยอดใช้ต่อ item ต่อเดือน (ตาราง breakdown รายเดือน) → เปลี่ยนเป็น `c.color`

**ยืนยันวิธีที่ถูกต้องจาก**: โค้ดส่งรายงาน Discord (บรรทัด ~2117) ที่อ่าน `barrelMap[c.color]` ถูกต้องอยู่แล้ว — ใช้เป็นต้นแบบยืนยันว่าโครงสร้างจริงคือ group-level color

**ผลกระทบหลังแก้**: เพราะเป็นการคำนวณสดจาก `dayRecords` ทุกครั้ง (ไม่มีข้อมูลถูกเขียนทับเสียหาย) แค่แก้โค้ดตัวเลขก็ถูกต้องทันทีโดยไม่ต้อง migrate ข้อมูลเก่า — แต่ **ตัวเลข "คงเหลือ" และยอดใช้เคมีของกลุ่ม COLOR-type ทุกตัวจะเปลี่ยนไป (ลดลง) ทันทีที่เปิดหน้าเว็บเวอร์ชันใหม่** ผู้ใช้ควรตรวจสอบยอดคงเหลือเคมีอีกครั้งหลังอัปเดต

## [plating_v4.html, plating_v5_B2.html, plating_v6_R1.html] 🔴 แก้บั๊กใหญ่ #2: จับคู่ชื่อเคมีไม่เจอเพราะช่องว่างไม่ตรงกัน — ✅ เสร็จสมบูรณ์ (23 ก.ค. 2569)
พบระหว่างตรวจ: เคมี **HYPRO200** (ชื่อใน Chem Master, code C-005) ไม่มีข้อมูล "ผลิต (เบิกใช้)" ในตารางเคลื่อนไหวเลย ทั้งที่อยู่กลุ่ม Black Chromate (K ดำ) เดียวกับ ALX-441A/ALX-441B ที่มีข้อมูลปกติ — เช็คแล้วพบว่าชื่อที่ตั้งไว้ใน `chemSettings` (หน้าตั้งค่าเคมี) สะกดว่า **"HYPRO 200"** (มีช่องว่าง) แต่ชื่อจริงใน Chem Master คือ **"HYPRO200"** (ไม่มีช่องว่าง) — ทุกฟังก์ชันเทียบชื่อด้วย `.trim().toLowerCase()` เฉยๆ (ตัดแค่ช่องว่างหัวท้าย ไม่ตัดช่องว่างกลางคำ) จึงมองว่าเป็นคนละชื่อกัน จับคู่ไม่เจอ คำนวณยอดใช้ออกมาเป็น 0 เสมอ

**แก้แล้ว**: เพิ่มฟังก์ชัน `chemNameKey(s)` — normalize ชื่อโดยตัดช่องว่างทุกจุดออก (ไม่ใช่แค่หัวท้าย) ก่อนเทียบ แทนที่ `.trim().toLowerCase()` แบบเดิมในทุกจุดที่ใช้เทียบชื่อเคมีสำหรับคำนวณยอดใช้ (ไม่แตะจุดที่ใช้ track การเปลี่ยนชื่อ/rename ซึ่งควร match แบบเป๊ะอยู่แล้ว) ครอบคลุม `calcAutoChemUsed`, `calcDayChemUsedLiter`, `calcDayChemRef`, `calcAutoChemUsed_forDay`, และ `exportChemCSV`
**พบเพิ่มระหว่างแก้**: `exportChemCSV` เดิมยิ่งแย่กว่านั้น — เทียบแค่ชื่อกลุ่ม (`c.name`) ไม่เคยเช็คชื่อสารย่อย (`chemicals[].name`) เลย ทำให้ CSV export ของเคมีที่เป็นสารย่อย (เช่น ALX-441A/B, HYPRO200) ออกมาเป็น 0 เสมอไม่ว่าชื่อจะตรงกันหรือไม่ — แก้ให้ match สารย่อยได้เหมือนฟังก์ชันอื่นแล้ว
**ข้อควรระวังสำหรับผู้ใช้**: บั๊กนี้แก้ได้แค่ "การจับคู่" — ถ้าใน Chem Master กับหน้าตั้งค่าเคมียังมีชื่อสะกดต่างกันแบบอื่นที่ไม่ใช่แค่ช่องว่าง (เช่น ตัวสะกดผิด, ใช้ตัวย่อคนละแบบ) จะยังจับคู่ไม่เจอเหมือนเดิม — แนะนำเช็คว่าชื่อเคมีทุกตัวใน Chem Master ตรงกับชื่อที่ตั้งไว้ในหน้าตั้งค่าเคมี (chem settings) แบบตัวต่อตัว

## [job_master.html] เพิ่มฟีเจอร์ "🧹 ปิดบิลเก่าเป็นชุด" (Bulk Close) — ✅ เสร็จสมบูรณ์ (23 ก.ค. 2569)
ปัญหาที่พบ: มีบิลรับเข้าเก่าจำนวนมาก (ก่อนมีระบบตัดสต็อก) ที่ "จ่ายออก=0" ตลอดแม้เกินกำหนดมาหลายสิบวัน เพราะของถูกใช้ไปจริงแล้วแต่ไม่เคยมี INV ผูกไว้ให้ตัดยอด ทำให้ "คงเหลือ" ในหน้า job_master.html โชว์เต็มจำนวนผิดๆ (พบเคสจริง P/No เดียวมีบิลค้างแบบนี้เป็นสิบใบ อายุ 1–33+ วัน)
**แก้แล้ว**: เพิ่มปุ่ม "🧹 ปิดบิลเก่าเป็นชุด" ที่แท็บภาพรวม (ข้าง CSV) เปิด modal ให้:
1. กรอกจำนวนวัน (default 30) — ค้นหาบิลที่รับเข้ามาแล้วเกิน N วัน และยังมีคงเหลือ > 0
2. โชว์ preview ตาราง (เลขบิล/ชิ้นงาน/ลูกค้า/อายุ/คงเหลือ) พร้อม checkbox เลือกได้เป็นรายบิล (default เลือกทั้งหมด) ก่อนยืนยัน — ไม่ใช่กดแล้วตัดทันทีแบบไม่มีจุดตรวจสอบ
3. ยืนยันแล้วมี native `confirm()` popup ซ้ำอีกชั้นกันกดพลาด
**ตรรกะการปิดบิล** (`bulkCloseBill()`): ตั้ง `job.qty`/`job.qtyKg` ให้เท่ากับ `totalOut` ที่คำนวณจาก `calcJob()` (ยอดที่เคยออก INV จริงแล้วเท่านั้น) — **ไม่ใช่ตั้งเป็น 0 เสมอไป** เผื่อบิลนั้นมีการออก INV จริงบางส่วนไปแล้ว จะปิดแค่ส่วนคงเหลือ ไม่ไปลบยอดที่ขายจริงออก ผลคือ `remain = totalIn - totalOut = 0` พอดี — อัปเดต `jobRecords` type `in` ตัวแรกให้ตรงกันด้วย พร้อม tag หมายเหตุ `[ปิดบิลอัตโนมัติ <วันที่> — ข้อมูลเก่าก่อนมีระบบตัดสต็อก]` ต่อท้ายทุกบิลที่ปิด เพื่อสอบกลับได้ภายหลัง
**ไม่กระทบ**: ไม่แตะ `blInvoices`/INV ที่ออกไปแล้วเลย เป็นการแก้แค่ "จำนวนรับเข้าที่บันทึกไว้" ให้ตรงกับความจริงเท่านั้น — แก้ไขย้อนกลับได้ทีหลังผ่าน "✏️ แก้ไขข้อมูลบิล" ถ้าปิดผิดบิล
**23 ก.ค. 2569 (แก้บั๊กต่อเนื่อง)**: หลังทดสอบจริง (ปิดไป 629 บิล) พบว่าบิลที่ปิดแล้ว (ตัด qty เหลือ 0) ยัง**ค้างสถานะ "เกินกำหนด" (สีแดง) ตลอดไป ไม่เปลี่ยนเป็น "จ่ายครบ"** — สาเหตุ: `getStatus()` เดิมเช็ค `isDone` จากเงื่อนไข `remain<=0 && totalIn>0` แต่บิลที่ปิดแล้วมี `totalIn=0` ด้วย (เพราะ qty เดิมก็คือ totalIn) เงื่อนไขนี้จึงไม่มีวันเป็นจริง ตกไปเช็คจำนวนวันแทน ได้ "เกินกำหนด" ทุกครั้ง แก้โดย:
1. เพิ่ม field `closed: true` ตอน `bulkCloseBill()` และให้ `getStatus()` เช็ค `job.closed` เป็นเงื่อนไขแรก คืนค่า "จ่ายครบ" ทันทีถ้าปิดบิลแล้ว
2. เพิ่ม fallback เช็คจากข้อความในหมายเหตุ (`note.indexOf('[ปิดบิลอัตโนมัติ')`) ด้วย — ครอบคลุมบิล 629 ใบที่ปิดไปแล้วก่อนแก้ (ไม่มี flag `closed` เพราะยังไม่มีตอนนั้น แต่มี tag ในหมายเหตุอยู่แล้วจากตอนปิด) ทำให้ status ของบิลเก่าที่ปิดไปแล้วถูกต้องทันทีโดยไม่ต้องรันซ้ำ
ผลคือ summary chip "จ่ายครบ"/"เกินกำหนด" ที่ด้านบนก็ถูกต้องตามไปด้วย (คำนวณจาก `getStatus()` เดียวกัน)

---

## 📌 แผนงานที่ตกลงกันไว้ (23 ก.ค. 2569)
**ลำดับแก้ปัญหาข้อมูลขัดแย้ง: เริ่มจาก job_master.html (jm) ก่อน แล้วค่อยสรุปแก้ bl_stock.html (bl) ทีหลัง**
- เหตุผล: ปัญหา "ข้อมูลขัดแย้ง" ที่เห็นใน bl_stock.html (WIP+Delivery เกินยอดรับเข้า, banner เตือนสีแดงที่เพิ่งทำ) กับปัญหาบิลเก่าค้างใน job_master.html (เช่น P/No 7G-3101-12 ที่มีบิลค้าง "จ่ายออก=0" เกินกำหนดหลายสิบวัน) **คาดว่าเป็นสาเหตุเดียวกัน** เพราะ bl_stock.html ดึง "รับเข้า" มาจาก jm โดยตรง
- ขั้นตอน: (1) ใช้ปุ่ม "🧹 ปิดบิลเก่าเป็นชุด" ใน job_master.html ไล่เคลียร์บิลเก่าที่ใช้หมดจริงแล้วแต่ยังโชว์คงเหลือค้าง ทีละ P/No หรือทั้งระบบ → (2) กลับมาเช็ค bl_stock.html อีกครั้งว่า banner แจ้งเตือนข้อมูลขัดแย้งลดลง/หายไปหรือไม่ → (3) ถ้ายังเหลือ warning อยู่ ค่อยไล่ดูเป็นกรณีๆ ว่าเป็นปัญหาอื่น (เช่น จับคู่ P/No ระหว่าง jm กับข้อมูลชุบ v4/5/6 ไม่ตรงกัน) ไม่ใช่ปัญหาบิลเก่าที่เคลียร์ไปแล้ว
- **สถานะ**: ยังไม่เริ่มไล่เคลียร์จริง — รอผู้ใช้ทดสอบเครื่องมือ Bulk Close ก่อน

## ⏳ รายการที่รู้ว่ายังค้างอยู่ (ยังไม่เสร็จ/รอตัดสินใจ)
- Firebase security rules: B2 กับ R1 project ยัง**เปิดกว้างอยู่** (ไม่ได้ล็อกเหมือน B1) — ต้องยืนยันว่า fb-auth.js อัปเดตครบทั้ง 18 ไฟล์และเทสต์แล้วหรือยัง
- ตาราง Backlog/Auto Scheduler ใน production_order.html — รอตัดสินใจว่าจะลบ UI ทิ้งหรือเก็บไว้
